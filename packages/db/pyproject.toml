[project]
name = "zyth-db"
version = "0.1.0"
description = "zyth.db - Database drivers"
requires-python = ">=3.10"
dependencies = ["zyth-runtime"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel.force-include]
"__init__.py" = "db/__init__.py"
