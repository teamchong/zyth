[project]
name = "zyth-runtime"
version = "0.1.0"
description = "Zyth Zig runtime library"
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]
