[project]
name = "zyth-http"
version = "0.1.0"
description = "zyth.http - HTTP client/server"
requires-python = ">=3.10"
dependencies = ["zyth-runtime"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel.force-include]
"__init__.py" = "http/__init__.py"
